c = int(input('введите сумму чисел: '))
d = int(input('введите произведение чисел: '))
for a in range(1, 1001):
    for b in range(1,1001):
        if (a + b)!= c or a*b != d:
            a+=1  
            b+=1
        else: print(a,b)
